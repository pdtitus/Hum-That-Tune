{
  "name": "backtrack-session-host",
  "version": "0.1.0",
  "private": true,
  "description": "Game-agnostic real-time session host for browser games",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
